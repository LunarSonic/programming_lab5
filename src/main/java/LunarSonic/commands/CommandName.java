package LunarSonic.commands;

/**
 * Enum, состоящий из названий всех команд
 */
public enum CommandName {
    add,
    add_if_max,
    add_if_min,
    clear,
    execute_script,
    exit,
    help,
    history,
    info,
    max_by_postal_address,
    remove_all_by_annual_turnover,
    remove_by_id,
    save,
    show,
    sum_of_annual_turnover,
    update
}
